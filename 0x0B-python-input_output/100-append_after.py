#!/usr/bin/python3
"""A search and update function."""


def append_after(filename="", search_string="", new_string=""):
    """Insert `new_string` into `filename` after each line containing
    `search_string`.

    Args:
        filename (str):
        search_string (str):
        new_string (str):
    """
    pass
